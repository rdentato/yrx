


ADT
 ** Once created an element will never be relocated in memory
 
VECTOR
 * Unlimited array of structures numbered from 0.
 
  vecNew(esz)
  vecGet(v,ndx)  -> elem
  vecSet(v,ndx,e) 
  vecCnt(v)
  
BLOCKS
 * 
   blkNew
   blkAdd
   blkDel

STACK
 *
   stkNew
   stkPush
   stkPop
   stkTop
   stkIsEmpty

MAP
 * Elements are structures whose first fields contain the key
   and the rest are 
 
   mapNew(elemsize, keysize)
   mapGet(v,key)
   mapSet(v,elem)
